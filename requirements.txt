# Upper bounds protect against breaking changes in major releases.
# Most critically numpy<2 — faster-whisper / ctranslate2 wheels are
# still numpy-1 ABI; numpy 2 silently breaks them at runtime.
faster-whisper>=1.0.3,<2.0
sounddevice>=0.4.6,<0.6
numpy>=1.24,<2.0
scipy>=1.10,<2.0
keyboard>=0.13.5,<0.14
pyperclip>=1.8.2,<2.0
pystray>=0.19.5,<0.20
Pillow>=12.2.0,<13.0
keyring>=25.0.0,<26.0
# Used directly for first-run model downloads (also transitive from faster-whisper).
huggingface-hub>=0.20,<1.0
# Modern Windows-look UI på topp av Tk. ~1 MB. Inga bilder/native deps.
customtkinter>=5.2.2,<6.0

# Snabbare resampling (valfritt, 3-4x snabbare än scipy):
# pip install soxr

# GPU/CUDA-stod (valfritt, stor nedladdning ~2.5GB):
# pip install torch --index-url https://download.pytorch.org/whl/cu124
